backend:
  name: github
  repo: scott-gr/oakhurst-dog-park
  branch: main

site_url: https://oakhurstdogpark.com

media_folder: static/assets
public_folder: /assets

collections:
  - name: blog
    label: Blog
    label_singular: Post
    description: The most recent Featured Post will appear on the home page.
    folder: /content/blog
    editor: 
      preview: true
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    path: "{{slug}}"
    fields:
      - { name: title, label: Title, widget: string }
      - { name: date, label: Date, widget: datetime }
      - { name: description, label: Description, widget: string, required: false }
      - { name: featuredpost, label: Featured Post?, widget: boolean}
      - { name: image, label: Cover Image, widget: image, required: false}
      - { name: body, label: Body, widget: markdown }

  # - name: announcements
  #   label: Announcements
  #   label_singular: Announcement
  #   description: Announcements will appear like a 'pinned' blog post in the bottom section of the front page.
  #   folder: /content/announcements
  #   editor: 
  #     preview: true
  #   create: true
  #   slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
  #   path: "{{slug}}"
  #   fields:
  #     - { name: title, label: Title, widget: string }
  #     - { name: date, label: Date, widget: datetime }
  #     - { name: description, label: Description, widget: string }
  #     - { name: image, label: Cover Image, widget: image, required: false, tagname: ""}
  #     - { name: body, label: Body, widget: markdown }
